export const Checkout_Line = ({top , width , borderColor, left}:{top:string; width:string; borderColor:string ; left:string;}) => {
  return <div style={{left, borderColor, width, top}} className="absolute rotate-0 border-[1px]
"></div>;
};
